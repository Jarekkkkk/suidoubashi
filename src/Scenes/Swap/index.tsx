import SwapContainer from '@/Containers/Swap';
import SwapPresentation from '@/Presentations/Swap';

const SwapScene = () => {
    return (
        <SwapContainer>
            <SwapPresentation />
        </SwapContainer>
    )
};

export default SwapScene;